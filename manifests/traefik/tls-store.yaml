---
# Default TLS store: makes the wildcard certificate the fallback certificate
# for all TLS routers on the websecure entrypoint.
apiVersion: traefik.io/v1alpha1
kind: TLSStore
metadata:
  name: default
  namespace: traefik
spec:
  defaultCertificate:
    secretName: wildcard-elmstreet79-tls
