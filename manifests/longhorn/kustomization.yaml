apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: longhorn-system
resources:
  - minio-secret-sealed.yaml
  # Bleibt vorerst neben minio-secret-sealed.yaml stehen. Das eigentliche
  # Umschalten passiert in values.yaml (defaultBackupStore), erst nach
  # verifiziertem Backup UND Restore auf dem NAS.
  - nas-cifs-secret-sealed.yaml
  - disable-local-path-default.yaml
  - recurring-backup-jobs.yaml
  - ingress.yaml
  - servicemonitor.yaml
  - node-config.yaml
