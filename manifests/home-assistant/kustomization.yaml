apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: home-assistant

resources:
  # PVs zu den aus dem NAS-Backup restaurierten Longhorn-Volumes.
  # Warum sie ins git gehoeren, steht im Kopf von pv.yaml.
  - pv.yaml
  - namespace.yaml
  - pvc.yaml
  - matter-pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - configmap-configuration.yaml
  - s3-archive-credentials-sealed.yaml
  - wol-ssh-key-sealed.yaml
  - backup-archive-cronjob.yaml
