# Long-lived access token from Home Assistant, created under
# Profile -> Security -> Long-lived access tokens. It is shown exactly once and
# stays valid until revoked in the same view.
#
# kubeseal --format=yaml --controller-namespace=kube-system \
#   < manifests/kube-prometheus-stack/home-assistant-token-unsealed.yaml \
#   > manifests/kube-prometheus-stack/home-assistant-token-sealed.yaml
---
apiVersion: v1
kind: Secret
metadata:
  name: home-assistant-prometheus-token
  namespace: monitoring
type: Opaque
stringData:
  token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...."
