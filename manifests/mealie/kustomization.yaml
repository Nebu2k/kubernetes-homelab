apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: mealie
resources:
  # PVs zu den aus dem NAS-Backup restaurierten Longhorn-Volumes.
  # Warum sie ins git gehoeren, steht im Kopf von pv.yaml.
  - pv.yaml
  - namespace.yaml
  - pvc.yaml
  - postgresql.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - mealie-secrets-sealed.yaml
  - networkpolicy.yaml
  - backup-cronjob.yaml
  - s3-mealie-backup-credentials-sealed.yaml
  - mealie-backup-token-sealed.yaml
