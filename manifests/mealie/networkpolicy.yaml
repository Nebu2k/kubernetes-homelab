---
# Ingress-only default-deny: nichts von außerhalb des Namespace erreicht die Pods,
# außer den unten explizit erlaubten Pfaden. Egress bleibt offen (Rezept-Import
# von fremden Webseiten, DNS).
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-ingress
  namespace: mealie
spec:
  podSelector: {}
  policyTypes:
    - Ingress
---
# Intra-Namespace frei: mealie <-> postgresql (gleiche Trust-Domain).
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-same-namespace
  namespace: mealie
spec:
  podSelector: {}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
---
# Traefik (ns traefik) darf die Web-UI auf 9000 erreichen.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-traefik-ingress
  namespace: mealie
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: mealie
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: traefik
      ports:
        - protocol: TCP
          port: 9000
