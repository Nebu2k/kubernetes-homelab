apiVersion: v1
kind: Service
metadata:
  name: training-engine-api
  namespace: training-engine
  labels:
    app.kubernetes.io/name: training-engine-api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app.kubernetes.io/name: training-engine-api
