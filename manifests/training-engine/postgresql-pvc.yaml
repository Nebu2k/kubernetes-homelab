apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: training-engine-postgresql-data
  namespace: training-engine
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
