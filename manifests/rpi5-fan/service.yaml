apiVersion: v1
kind: Service
metadata:
  name: rpi5-fan
  namespace: rpi5-fan
  labels:
    app: rpi5-fan
spec:
  selector:
    app: rpi5-fan
  ports:
    - name: metrics
      port: 9101
      targetPort: metrics
