apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: minio

resources:
  - namespace.yaml
  - credentials-sealed.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
