apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Kein namespace.yaml: "monitoring" gehoert der kube-prometheus-stack-App und
# wird von ihr angelegt. Deshalb steht die Application dieser App auch auf
# CreateNamespace=false und auf einer hoeheren sync-wave.
namespace: monitoring

resources:
  - s3-backup-monitor-credentials-sealed.yaml
  - freshness-cronjob.yaml
