apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rpi5-net-tuning
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/Nebu2k/kubernetes-homelab
    targetRevision: main
    path: manifests/rpi5-net-tuning
  destination:
    server: https://kubernetes.default.svc
    namespace: rpi5-net-tuning
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      # The namespace comes from namespace.yaml rather than from ArgoCD: it
      # needs the PodSecurity labels, which an ArgoCD-created one would lack.
      - CreateNamespace=false
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
