apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: backup-monitor
  namespace: argocd
  annotations:
    # Nach kube-prometheus-stack (6): der Namespace "monitoring" und die
    # Alert-Regel, die den fehlgeschlagenen Job meldet, gehoeren dorthin.
    argocd.argoproj.io/sync-wave: "11"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/Nebu2k/kubernetes-homelab
    targetRevision: main
    path: manifests/backup-monitor
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
