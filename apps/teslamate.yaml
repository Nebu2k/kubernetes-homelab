apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: teslamate
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "5"
spec:
  project: default
  source:
    repoURL: https://github.com/Nebu2k/kubernetes-homelab
    targetRevision: main
    path: manifests/teslamate
  destination:
    server: https://kubernetes.default.svc
    namespace: teslamate
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
