# Cloudflare DNS Configuration
# Copy this file to .env and fill in your values
# cp .env.example .env

# Your Cloudflare domain (e.g., example.com, elmstreet79.de)
DOMAIN=example.com

# DNS target for CNAME records (e.g., your DynDNS hostname or LoadBalancer IP)
# This can be a hostname (myserver.dyndns.org) or an IP address (192.168.2.249)
TARGET=myserver.dyndns.org

# Cloudflare Zone ID
# Find at: Cloudflare Dashboard > Select Domain > Overview (right sidebar)
ZONE_ID=your_zone_id_here

# Cloudflare API Token
# Create at: Cloudflare Dashboard > My Profile > API Tokens > Create Token
# Required permissions: Zone.DNS Edit
API_TOKEN=your_api_token_here
