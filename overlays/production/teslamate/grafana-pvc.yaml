apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: teslamate-grafana
  namespace: teslamate
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
