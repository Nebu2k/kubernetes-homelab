---
# Example unsealed secret - replace with sealed version
# To create a sealed secret:
# 1. Create your secret: kubectl create secret generic psitransfer-secret --from-literal=upload-pass='your-secure-password' --dry-run=client -o yaml > psitransfer-secret-unsealed.yaml
# 2. Seal it: kubeseal --format=yaml < psitransfer-secret-unsealed.yaml > psitransfer-secret-sealed.yaml
# 3. Delete the unsealed version: rm psitransfer-secret-unsealed.yaml
apiVersion: v1
kind: Secret
metadata:
  name: psitransfer-secret
  namespace: psitransfer
type: Opaque
stringData:
  upload-pass: "changeme"  # Replace with your secure upload password
