---
# Example unsealed secret - replace with sealed version
# Seal it: kubeseal --format=yaml < psitransfer-secret-unsealed.yaml > psitransfer-secret-sealed.yaml

apiVersion: v1
kind: Secret
metadata:
  name: psitransfer-secret
  namespace: psitransfer
type: Opaque
stringData:
  upload-pass: "changeme"  # Replace with your secure upload password
