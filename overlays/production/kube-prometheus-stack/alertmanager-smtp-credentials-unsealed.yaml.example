# This is a template for AlertManager Amazon SES credentials
# 
# Usage:
# 1. Copy this file and remove the .example extension:
#    cp alertmanager-ses-unsealed.yaml.example alertmanager-ses-unsealed.yaml
#
# 2. Fill in your actual SES credentials below
#
# 3. Seal the secret (requires cluster connection):
#    kubeseal --format=yaml --controller-namespace=kube-system \
#      < alertmanager-ses-unsealed.yaml \
#      > alertmanager-ses-sealed.yaml
#
# 4. Delete the unsealed version:
#    rm alertmanager-ses-unsealed.yaml
#
# 5. Commit the sealed version
#
# Amazon SES SMTP Credentials:
# Get from: AWS Console → SES → SMTP Settings → Create SMTP Credentials
# Note: Also verify your sender email in SES (alerts@elmstreet79.de)

apiVersion: v1
kind: Secret
metadata:
  name: alertmanager-smtp-credentials
  namespace: monitoring
type: Opaque
stringData:
  smtp_from: "alerts@elmstreet79.de"
  smtp_to: "alerts@elmstreet79.de"
  smtp_smarthost: "email-smtp.eu-west-1.amazonaws.com:587"
  smtp_username: "YOUR_SES_SMTP_USERNAME"
  smtp_password: "YOUR_SES_SMTP_PASSWORD"
