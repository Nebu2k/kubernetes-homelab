---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: homepage-beszel
  namespace: homepage
spec:
  encryptedData:
    username: PLACEHOLDER_REPLACE_WITH_SEALED_USERNAME
    password: PLACEHOLDER_REPLACE_WITH_SEALED_PASSWORD
  template:
    metadata:
      name: homepage-beszel
      namespace: homepage
    type: Opaque
