---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fr24
  namespace: private-services
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
spec:
  ingressClassName: nginx
  rules:
    - host: fr24.elmstreet79.de
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: fr24
                port:
                  number: 80
