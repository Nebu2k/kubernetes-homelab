---
# External Service (pointing to Pi5 AdGuard Sync)
apiVersion: v1
kind: Service
metadata:
  name: adguard-sync-pi5
  namespace: private-services
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8081
      protocol: TCP
      name: http
---
apiVersion: v1
kind: Endpoints
metadata:
  name: adguard-sync-pi5
  namespace: private-services
subsets:
  - addresses:
      - ip: 192.168.2.9
    ports:
      - port: 8081
        name: http
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: adguard-sync-pi5
  namespace: private-services
  annotations:
    cert-manager.io/cluster-issuer: internal-ca-issuer
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - adguard-sync.elmstreet79.de
      secretName: adguard-sync-pi5-tls
  rules:
    - host: adguard-sync.elmstreet79.de
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: adguard-sync-pi5
                port:
                  number: 80
