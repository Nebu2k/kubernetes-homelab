---
apiVersion: v1
kind: Service
metadata:
  name: wazuh-dashboard
  namespace: private-services
spec:
  type: ClusterIP
  ports:
    - name: https
      port: 443
      targetPort: 443
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: wazuh-dashboard
  namespace: private-services
subsets:
  - addresses:
      - ip: 192.168.2.15
    ports:
      - name: https
        port: 443
        protocol: TCP
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: wazuh-dashboard
  namespace: private-services
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`wazuh.elmstreet79.de`)
      kind: Rule
      services:
        - name: wazuh-dashboard
          port: 443
          scheme: https
          serversTransport: insecure-transport
  tls:
    secretName: wazuh-dashboard-tls
---
# Dummy Ingress for DNS sync and cert-manager
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wazuh-dashboard-dns
  namespace: private-services
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - wazuh.elmstreet79.de
      secretName: wazuh-dashboard-tls
  rules:
    - host: wazuh.elmstreet79.de
      http:
        paths:
          - path: /__dns_sync_only__
            pathType: Exact
            backend:
              service:
                name: wazuh-dashboard
                port:
                  number: 443
