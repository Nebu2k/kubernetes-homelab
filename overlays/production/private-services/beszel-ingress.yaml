---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: beszel-pi5
  namespace: private-services
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
spec:
  ingressClassName: nginx
  rules:
    - host: beszel.elmstreet79.de
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: beszel-pi5
                port:
                  number: 80
