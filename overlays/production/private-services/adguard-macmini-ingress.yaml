---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: adguard-macmini
  namespace: private-services
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
spec:
  ingressClassName: nginx
  rules:
    - host: adguard-macmini.elmstreet79.de
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: adguard-macmini
                port:
                  number: 80
