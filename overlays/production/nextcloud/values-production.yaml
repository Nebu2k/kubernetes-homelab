# Production-specific Nextcloud Helm Values
# These values override/extend the base values.yaml

nextcloud:
  # Production domain
  host: nextcloud.elmstreet79.de
  
  # Trusted domains for this deployment
  trustedDomains:
    - nextcloud.elmstreet79.de