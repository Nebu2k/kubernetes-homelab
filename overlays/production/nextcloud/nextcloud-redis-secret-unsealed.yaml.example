---
# Example unsealed secret - replace with sealed version
# Seal it: kubeseal --format=yaml < nextcloud-redis-secret-unsealed.yaml > nextcloud-redis-secret-sealed.yaml

apiVersion: v1
kind: Secret
metadata:
  name: nextcloud-redis
  namespace: nextcloud
type: Opaque
stringData:
  redis-password: "changeme"  # Replace with your secure Redis password
