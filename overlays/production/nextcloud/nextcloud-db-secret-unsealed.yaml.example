---
# Example unsealed secret - replace with sealed version
# Seal it: kubeseal --format=yaml < nextcloud-db-secret-unsealed.yaml > nextcloud-db-secret-sealed.yaml

apiVersion: v1
kind: Secret
metadata:
  name: nextcloud-postgresql
  namespace: nextcloud
type: Opaque
stringData:
  password: "changeme"           # Replace with your secure PostgreSQL password
  postgres-password: "changeme"  # Replace with your secure PostgreSQL admin password
