# Longhorn distributed storage values
persistence:
  defaultClass: true
  defaultClassReplicaCount: 2
  
defaultSettings:
  backupTarget: "s3://longhorn@us-east-1/https://192.168.2.9:9300"
  backupTargetCredentialSecret: "longhorn-s3-secret"
  defaultReplicaCount: "2"

# Disable preUpgrade hook (causes issues with ArgoCD)
preUpgradeChecker:
  jobEnabled: false
  
# NodePort for UI access
service:
  ui:
    type: NodePort
    nodePort: 30080
