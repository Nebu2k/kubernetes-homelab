# Homepage Dashboard Helm values
# https://github.com/gethomepage/homepage

# Enable reloader annotation
podAnnotations:
  reloader.stakater.com/auto: "true"

# Schedule only on worker nodes
nodeSelector:
  node-role.kubernetes.io/worker: "worker"

# Service configuration
service:
  main:
    ports:
      http:
        port: 3000

# Ingress disabled (will be added in overlay)
ingress:
  main:
    enabled: false

# Persistence for config (optional, can use ConfigMaps instead)
persistence:
  logs:
    enabled: true
    type: emptyDir
    mountPath: /app/config/logs

# Resources
resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    memory: 256Mi

# ServiceAccount - let Helm chart create it
serviceAccount:
  create: true

# RBAC - let Helm chart create it
rbac:
  create: true

# Config will be provided via ConfigMaps in overlay
config:
  useExistingConfigMap: homepage-config
