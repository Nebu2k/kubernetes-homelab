apiVersion: v1
kind: Namespace
metadata:
  name: pihole
  labels:
    name: pihole
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: pihole-sa
  namespace: pihole
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: pihole-role
rules:
- apiGroups: [""]
  resources: ["services", "endpoints", "pods"]
  verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: pihole-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: pihole-role
subjects:
- kind: ServiceAccount
  name: pihole-sa
  namespace: pihole
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pihole-config-pvc
  namespace: pihole
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pihole-dnsmasq-pvc
  namespace: pihole
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: pihole-dnsmasq-config
  namespace: pihole
data:
  02-custom-vlans.conf: |
    # Custom VLAN configuration for conditional forwarding
    # Main LAN 192.168.2.0/24
    server=/elmstreet79.de/192.168.2.1
    rev-server=192.168.2.0/24,192.168.2.1
    
    # VLAN 192.168.4.0/24
    rev-server=192.168.4.0/24,192.168.2.1
    
    # VLAN 192.168.5.0/24
    rev-server=192.168.5.0/24,192.168.2.1
    
    # Enable local domain resolution for all VLANs
    local=/elmstreet79.de/
    domain=elmstreet79.de
    expand-hosts
---
apiVersion: v1
kind: Secret
metadata:
  name: pihole-secret
  namespace: pihole
type: Opaque
stringData:
  WEBPASSWORD: "admin123"  # Change this password!
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pihole
  namespace: pihole
  labels:
    app: pihole
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pihole
  template:
    metadata:
      labels:
        app: pihole
    spec:
      serviceAccountName: pihole-sa
      securityContext:
        runAsUser: 0
        runAsGroup: 0
        fsGroup: 0
      containers:
      - name: pihole
        image: pihole/pihole:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 53
          name: dns-udp
          protocol: UDP
        - containerPort: 53
          name: dns-tcp
          protocol: TCP
        - containerPort: 80
          name: web
          protocol: TCP
        env:
        - name: TZ
          value: "Europe/Berlin"
        - name: PIHOLE_DNS_
          value: "1.1.1.1;1.0.0.1"
        - name: DNSMASQ_LISTENING
          value: "single"
        - name: CONDITIONAL_FORWARDING
          value: "true"
        - name: CONDITIONAL_FORWARDING_IP
          value: "192.168.2.1"
        - name: CONDITIONAL_FORWARDING_DOMAIN
          value: "elmstreet79.de"
        - name: CONDITIONAL_FORWARDING_REVERSE
          value: "192.168.2.0/24"
        # Additional VLANs for conditional forwarding
        - name: REV_SERVER
          value: "true"
        - name: REV_SERVER_DOMAIN
          value: "elmstreet79.de"
        - name: REV_SERVER_TARGET
          value: "192.168.2.1"
        - name: REV_SERVER_CIDR
          value: "192.168.2.0/24,192.168.4.0/24,192.168.5.0/24"
        - name: WEBPASSWORD_FILE
          value: "/etc/pihole/WEBPASSWORD"
        volumeMounts:
        - name: pihole-config
          mountPath: /etc/pihole
        - name: pihole-dnsmasq
          mountPath: /etc/dnsmasq.d
        - name: pihole-secret-volume
          mountPath: /tmp/WEBPASSWORD
          subPath: WEBPASSWORD
        - name: pihole-dnsmasq-custom-config
          mountPath: /etc/dnsmasq.d/02-custom-vlans.conf
          subPath: 02-custom-vlans.conf
        resources:
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 100m
            memory: 128Mi
        livenessProbe:
          httpGet:
            path: /admin/
            port: 80
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /admin/
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        lifecycle:
          postStart:
            exec:
              command:
                - /bin/sh
                - -c
                - |
                  if [ -f /tmp/WEBPASSWORD ]; then
                    cp /tmp/WEBPASSWORD /etc/pihole/WEBPASSWORD
                    chmod 600 /etc/pihole/WEBPASSWORD
                  fi
      volumes:
      - name: pihole-config
        persistentVolumeClaim:
          claimName: pihole-config-pvc
      - name: pihole-dnsmasq
        persistentVolumeClaim:
          claimName: pihole-dnsmasq-pvc
      - name: pihole-secret-volume
        secret:
          secretName: pihole-secret
      - name: pihole-dnsmasq-custom-config
        configMap:
          name: pihole-dnsmasq-config
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: pihole-dns
  namespace: pihole
  annotations:
    metallb.universe.tf/allow-shared-ip: pihole-svc
spec:
  selector:
    app: pihole
  type: LoadBalancer
  loadBalancerIP: 192.168.2.250
  ports:
  - port: 53
    targetPort: 53
    protocol: UDP
    name: dns-udp
  - port: 53
    targetPort: 53
    protocol: TCP
    name: dns-tcp
---
apiVersion: v1
kind: Service
metadata:
  name: pihole-web
  namespace: pihole
spec:
  selector:
    app: pihole
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30081
    protocol: TCP
    name: web
